# 统一以 LF 存储和检出。
#
# 背景：换行符不能依赖各人机器上的 core.autocrlf 设置，否则
#   - Windows 检出为 CRLF 时 `gofmt -l .` 会把所有文件报成未格式化
#   - shell 脚本会因 CRLF 出现 "bad interpreter: /bin/bash^M"
# 本仓库同时支持 Windows 与 Linux，故显式固定为 LF。
* text=auto eol=lf

# 二进制文件禁止换行转换
*.png  binary
*.ico  binary
*.syso binary
*.exe  binary

# Windows 批处理保持 CRLF，否则 cmd.exe 解析可能出错
*.bat text eol=crlf
*.cmd text eol=crlf
