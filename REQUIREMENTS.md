# 需求文档

> 串口调试工具 v0.6.4 需求清单，随源码同步维护。
> 状态图例：`✓` 已实现；`❌` 未实现（规划中）。

## 目录

- [公共](#公共)
- [守护进程](#守护进程)（含 [进程生命周期管理](#进程生命周期管理)）
- [GUI](#gui)
- [网络调试](#网络调试)（规划中）
- [CLI](#cli)
- [MCP](#mcp)
- [Web 客户端](#web-客户端)（规划中）
- [Python 版对照](#python-版对照)

## 公共

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | 操作日志 | ✓ | `HH:MM:SS [类别] 消息`，标记来源 (GUI/CLI/MCP) |
| 2 | 请求来源标识 | ✓ | `Request.Source` 字段 (`gui` / `cli` / `mcp`) |
| 3 | 十六进制发送支持 | ✓ | 含空格自动去除 |
| 4 | 串口打开超时保护 | ✓ | 8s |
| 5 | 写入超时保护 | ✓ | 3s |
| 6 | 字节模式管道 | ✓ | 同步 I/O，无 OVERLAPPED |
| 7 | 换行分隔 JSON-RPC 协议 | ✓ | `\n` 分帧 |
| 8 | 版本号统一 | ✓ | 全部 4 个可执行文件 + 文档统一版本号 |

## 守护进程

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | 独立运行 | ✓ | 无参数启动即守护进程模式 |
| 2 | 单实例 | ✓ | Windows 互斥体 `Global\serial-tool-daemon` |
| 3 | 双启动模式 | ✓ | 无参数前台控制台，`--silent` 后台静默 |
| 4 | 启停 | ✓ | GUI/CLI/MCP 通过 `shutdown` IPC 优雅关闭 |
| 5 | 端口列表 | ✓ | `ports` 读缓存，`ports.refresh` 刷新 |
| 6 | 一线程一串口 | ✓ | 每个 connected 进程独立 readLoop goroutine |
| 7 | 历史缓冲区 | ✓ | 共享内存环形缓冲区，5MB/进程，毫秒时间戳+Hex |
| 8 | 异步发送 | ✓ | `writeLoop` goroutine + `sendCh` cap 32 |
| 9 | 硬件探针 | ✓ | 后台 2s 间隔自动扫描，变化时广播 `ports-changed` |
| 10 | I/O 统计 | ✓ | `statsLoop` 每秒推送 `stats-rate`，readLoop/writeLoop 推送 `stats-count` |
| 11 | 连接 PID 追踪 | ✓ | `GetNamedPipeClientProcessId`，同 PID 多管道只记一次 |
| 12 | 端口命令分离 | ✓ | `ports` 只读缓存，`ports.refresh` 主动刷新 |
| 13 | 订阅式事件推送 | ✓ | 客户端 `subscribe` IPC 声明订阅，daemon 按订阅推送 |
| 14 | process-changed 事件 | ✓ | 进程创建/销毁/连接/断开/模式切换/查看变更时广播；事件自带进程列表 |
| 15 | 心跳机制 | ✓ | `ping` IPC 静默检测，3 次失败断开 |
| 16 | 3 管道注册 | ✓ | 持久客户端 `register` IPC 建立三条独立管道 |
| 17 | 写入超时自动清理 | ✓ | `stopOnce` 确保 stopIO 只执行一次 |
| 18 | 发送操作静默 | ✓ | 串口发送数据不记录后台操作日志 |
| 19 | 共享内存发送队列 | ✓ | `serial-tool-sendq-{id}` 1MB/进程，客户端写共享内存、IPC 触发 |
| 20 | 自动发送引擎 | ✓ | 守护进程 autoSendLoop goroutine，单条模式+队列模式 |
| 21 | 自动发送固定频率 | ✓ | 从上条开始时刻计时，Timer 支持动态修改间隔 |
| 22 | 自动发送 tx 通知 | ✓ | 自动发送播送 tx 事件+记录历史，产生 send-error 事件 |
| 23 | 发送数据共享内存路径 | ✓ | 所有客户端 send 数据走共享内存，IPC 仅触发 send.trigger |
| 24 | 自动发送运行中修改 | ✓ | 间隔修改 autosend.interval，数据更新写入 sendq 不清空 |
| 25 | 清空历史 | ✓ | `session.clearhistory` IPC 重置环形缓冲区 |
| 26 | 串口切换 | ✓ | `process.switch` IPC，关闭旧端口→打开新端口，保留进程历史 |
| 27 | 端口转发 | ✓ | `process.create mode=forward`，双向透传，PC 被动监控，"1"/"2" 方向事件 |
| 28 | 设备端口探测 | ✓ | `ports.probe` IPC，TOML 规则配置，发探针帧+匹配响应识别设备类型 |
| 29 | 探测防重入 | ✓ | 探测自动跳过 portMap 中已连接端口 |
| 30 | 订阅立即推送 | ✓ | subscribe IPC 后立即推送当前状态，空参数推送全部已订阅事件 |
| 31 | 进程模式字段 | ✓ | Process 新增 `mode` 字段（single / forward），统一单端口与转发进程模型 |
| 32 | 进程模式切换 | ✓ | `process.setmode` IPC，需 idle 状态，可在 single ↔ forward 间切换 |
| 33 | 端口声明 | ✓ | `process.create` 新增 `connect` 参数，`false` 时存储完整配置不打开端口，全客户端可见 |
| 34 | 声明转发 | ✓ | forward 模式声明双端口配置不启动转发，之后 `connect` 可省略端口参数 |

### 推送事件

| 事件 | 触发 | 说明 |
|------|------|------|
| `rx` / `tx` | 串口接收 / 发送 | 数据事件，含进程 ID 与数据帧 |
| `1` / `2` | 转发模式端口 A / B | 方向事件，PC 被动监控 |
| `send-error` | 发送失败 | 含进程 ID 与错误信息 |
| `ports-changed` | 端口热插拔 | 硬件探针 2s 间隔扫描，变化时广播 |
| `process-changed` | 进程创建 / 销毁 / 连接 / 断开 / 模式切换 / 查看变更 | 事件自带进程列表 |
| `daemon-shutdown` | 守护进程关闭 | `shutdown` IPC 触发 |
| `stats-count` | I/O 计数变化 | readLoop / writeLoop 推送 |
| `stats-rate` | 每秒速率 | statsLoop 每秒推送 |
| `clients-changed` | 客户端连接 / 断开 | 含客户端列表 |
| `multistr-changed` | 多字符串发送状态 / 条目变更 | 发送状态变更、条目更新时广播 |

### 进程生命周期管理

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | 打开串口与创建进程分离 | ✓ | `process.create` 可选 port |
| 2 | 可创建空闲进程 | ✓ | 不传 port → idle 状态，无 I/O goroutine |
| 3 | 空闲进程绑定串口 | ✓ | `process.connect` + `portMap` 双向映射去重 |
| 4 | 显式销毁 | ✓ | `process.destroy`，有连接则先 disconnect |
| 5 | 销毁时先关串口 | ✓ | stopIO → 移除 portMap → 删除 Process |
| 6 | 关串口不销毁进程 | ✓ | `process.disconnect` → idle，可重新 connect |
| 7 | 双向端口映射 | ✓ | `processes map[pid]*Process` + `portMap map[port]pid` |
| 8 | 串口去重 | ✓ | create 带 port 或 connect 时查 portMap |
| 9 | 查询返回所有进程 | ✓ | `process.list` 含 idle 和 connected，含 viewers 计数和端口配置 |
| 10 | shutdown 先关所有串口 | ✓ | DestroyAll → 广播 → 关闭管道 |
| 11 | 进程状态变更回调 | ✓ | `onProcessChanged()` 在四类变更操作末尾调用 |
| 12 | stats 事件订阅推送 | ✓ | statsLoop 每秒推送 stats 事件到订阅客户端 |
| 13 | 串口切换 | ✓ | SwitchPort 关闭旧端口→打开新端口，保留 portMap 映射 |
| 14 | 端口转发 | ✓ | Create("forward", ...) 打开双端口双向拷贝，"1"/"2" 方向事件广播 |
| 15 | 进程模式切换 | ✓ | SetMode 切换 single ↔ forward，需 idle 状态 |
| 16 | 进程查看追踪 | ✓ | 守护进程自动追踪：GUI 注册时观察全部进程；CLI/MCP 在 connect/send 时自动递增；新增 `process.unwatch` / `process.watched` IPC；断连时批量清理 |

## GUI

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | 启动/停止守护进程 | ✓ | 状态栏按钮，所有客户端统一入口 |
| 2 | 心跳连接管理 | ✓ | 在线时无轮询，5s 心跳静默检测 |
| 3 | 即时断连检测 | ✓ | readSubLoop/readRespLoop 管道断裂立即回调 |
| 4 | 离线探测 | ✓ | tasklist 每 1s 检测，快速重试 3×500ms |
| 5 | 多标签页 | ✓ | 全部 daemon 进程（含 idle）实时同步；新建标签与 syncDaemonSessions 竞态去重 |
| 6 | 串口参数配置 | ✓ | 端口、波特率，详细设置弹窗；设置标签宽度适配多语言；转发 P2 标签完整翻译 |
| 7 | 空闲进程+绑定模式 | ✓ | GUI 创建空闲进程，再 connect 到端口 |
| 8 | 打开串口 | ✓ | 空闲进程上 connect 到选定端口 |
| 9 | 关闭串口不清空历史 | ✓ | disconnect 保留空闲进程和环形缓冲区历史 |
| 10 | 关闭标签页 | ✓ | = 销毁进程，清空对应历史 |
| 11 | 文本/Hex 双模显示 | ✓ | 单键切换，时间戳换行+方向箭头（Tx ↾ / Rx ⇃） |
| 12 | 文本/Hex 双模发送 | ✓ | 单键切换，Hex 模式按键黄色高亮 |
| 13 | 多行文本发送 | ✓ | 多行 textarea，非显示字符方块占位 |
| 14 | 循环发送 | ✓ | 复选框开关，勾选即发，间隔 5ms 起可配 |
| 15 | 追加换行 | ✓ | 文本 `\r\n`，Hex `0D0A` |
| 16 | 端口刷新 | ✓ | 手动 + `ports-changed` 事件+描述自动更新 |
| 17 | 清空显示 | ✓ | v0.6.3 起本地化：仅清 GUI 视图，不清环形缓冲区与磁盘文件（见 #117）；daemon 侧保留 `session.clearhistory` IPC 供 CLI 等客户端使用 |
| 18 | 深色/浅色主题 | ✓ | 跟随系统 / 深色 / 浅色 循环切换 |
| 19 | 离线遮罩 | ✓ | 守护进程离线时覆盖 |
| 20 | 线程/进程查看 | ✓ | 菜单弹窗显示 goroutine 数+进程详情 |
| 21 | 历史记录 | ✓ | 环形缓冲区+本地缓存，切换标签即时渲染 |
| 22 | GUI↔CLI 进程同步 | ✓ | CLI/MCP 打开的进程显示为 `[CLI]` 标签 |
| 23 | rx 按进程过滤 | ✓ | 仅当前标签页数据显示 |
| 24 | 3 管道持久连接 | ✓ | 请求/响应/事件分离，事件驱动无轮询 |
| 25 | 首次连接自动创建空闲进程 | ✓ | — |
| 26 | 进程状态变更即时同步 | ✓ | process-changed 事件驱动，广播自带进程列表免除额外 GetSessions 调用 |
| 27 | 外部启动守护进程自动检测 | ✓ | checkOffline 调用 tryConnect 前重置 connectRetries |
| 28 | 状态栏统计 | ✓ | ↾Tx/Rx 字节数+↾Tx/Rx 速率(百分比)，stats 事件驱动 |
| 29 | 可拖动分割线 | ✓ | 历史区与发送区按比例缩放，可完全折叠一侧 |
| 30 | 控件悬浮+模式变色 | ✓ | 发送控件栏悬浮透明，Hex 模式按键黄色 |
| 31 | 自动发送 UI | ✓ | 复选框开关，启动/停止，运行中修改间隔和数据 |
| 32 | 文本/Hex 独立缓冲区 | ✓ | 切换发送模式时各自保留输入内容 |
| 33 | 每标签独立发送状态 | ✓ | 切换标签时保存/恢复发送框、格式、追加换行、自动发送等 |
| 34 | 历史窗口渲染 | ✓ | 根据显示区高度动态计算可见条数×1.5，滚动加载更早记录 |
| 35 | 发送栏信息 | ✓ | 右侧显示数据大小和预计耗时（如 100B ≈8.7ms） |
| 36 | 速率颜色 | ✓ | 串口占用率 ≥80% 橙色，≥90% 红色 |
| 37 | 端口占用标记 | ✓ | 其他标签已连接端口在下拉框中显示橙色 |
| 38 | 字节显示精度 | ✓ | k 级 2 位小数，M 级 3 位小数 |
| 39 | 设置持久化 | ✓ | 界面模式、分割比例、串口参数、尾部追加等保存到 settings.ini |
| 40 | 编码格式全局设置 | ✓ | ASCII / GB2312 / UTF-8，菜单二级悬浮选择，影响显示和发送 |
| 41 | Hex 显示样式 | ✓ | 0x 前缀、大小写、空格/逗号分隔，仅影响 Hex 模式；对号状态启动时同步设置文件 |
| 42 | 容错解码 | ✓ | 手动逐字节解析，无效字节 /FF 转义，不产 � |
| 43 | 可见控制字符 | ✓ | v0.6.2：单节点+::before标记，可选不可复制 |
| 44 | 消息去重 | ✓ | 缓存指纹去重 + 系统消息相邻去重 |
| 45 | 主题切换 | ✓ | 菜单栏右侧按键，深色/浅色/跟随系统循环切换；SVG `var(--fg)` 解决 WebView2 继承失效；跟随系统半实心圆；太阳八向射线 |
| 46 | 空闲标签显示声明配置 | ✓ | idle 进程若有声明端口，标签显示 `COM3@115200 [已声明]`，syncedParams 持久化 |
| 47 | 标签查看计数 | ✓ | 标签按钮显示彩色终端数量，自身=1 隐藏 |
| 48 | 菜单 i18n 全量覆盖 | ✓ | 文件/Hex显示/文本显示子菜单全量翻译；设置标签加宽防止折行；9 种语言 |
| 49 | 菜单空隙 | ✓ | 文件/后端/帮助菜单选项 30px 对号空隙恢复；分割线避让文字起线；语言菜单避免双空隙 |
| 50 | 滚动锁定 | ✓ | 单端口/转发模式文本 Hex 切换键后加入自动滚动/锁定滚动文字按键；锁定时禁止自动滚底 |
| 51 | 转发 P1/P2 按钮端口颜色 | ✓ | 文本模式下 P1 蓝色 / P2 橙色文字+边框，Hex 模式实心背景 |
| 52 | 多字符串面板 | ✓ | 历史框和发送框右侧可拖出面板，工具菜单勾选开关，展开/收起双向拖动同步 |
| 53 | 工具菜单 | ✓ | 菜单栏"设置"后新增，内含多字符串开关（对号指示展开状态） |
| 54 | 开发人员工具 | ✓ | 菜单"帮助 → 开发人员工具"入口 + Ctrl+Shift+F12 快捷键，构建需 `-devtools` 标志 |
| 55 | 外部 i18n 翻译 | ✓ | exe 同目录 `i18n/{lang}.json` 覆盖内置或新增语言，`_dir` 声明 RTL，打开菜单防抖刷新 |
| 56 | 多字符串快捷发送 | ✓ | 多字符串面板支持添加/编辑/删除预设条目，点击条目直接发送；队列发送/循环发送由 daemon 统一驱动 |
| 57 | 历史记录持久化 | ✓ | 双写模式（环形缓冲区 + `history/*.log`），惰性创建文件（首次实际数据到达时），关闭进程保留 |
| 58 | 历史记录搜索 | ✓ | IPC `history.search` + CLI `history-search`，二进制帧顺序扫描匹配，分页返回 |
| 59 | 串口连接来源识别 | ❌ | 端口列表中标明已占用端口的连接来源（进程/线程/客户端类型），自身连接标注标签名 |
| 60 | RX/TX 显示独立切换 | ✓ | v0.5.9：`txDisplayMode` 独立于 `displayMode`，发送行单独控制文本/Hex 显示 |
| 61 | 发送框工具栏三按键分立 | ✓ | v0.5.9：接收显示切换 + 发送显示切换 + 发送格式切换，参照 P1/P2 模式 |
| 62 | 工具栏水平滚动 | ✓ | v0.5.9：隐藏滚动条，鼠标拖拽滑动，两侧悬浮渐变箭头指示方向，`ResizeObserver` 动态显隐 |
| 63 | 按钮文字精简 | ✓ | v0.5.9：箭头 + 字/H 短标签，悬浮提示切换说明，`data-wide` ghost 自适应宽度防跳动 |
| 64 | SVG 图标系统 | ✓ | v0.5.9：键化 ICONS 库，640×640 画布，`data-icon` 声明式注入 |
| 65 | 内置主题系统 | ✓ | v0.5.9：Go embed `config/themes/`（system.json，dark/light 双模式），CSS 变量覆盖 + Wails 绑定 |
| 66 | 语言字体绑定 | ✓ | v0.5.9：i18n `_font` 字段指定语言首选字体，`document.fonts.check()` 检测无字体回退默认 |
| 67 | 发送字节自适应单位 | ✓ | v0.5.9：B/kB(2位小数)/MB(1位小数) 自适应，右对齐，最小宽度 80px |
| 68 | 左侧区域最小宽度 | ✓ | v0.5.9：`.left-area` min-width 300px 防止历史框+发送框被 quick panel 挤压 |
| 69 | 标签页完整组件化 | ✓ | v0.6.0：TabPage 独立 DOM 子树（`.tab-page > .tab-page-inner`），per-tab 完整状态保存/恢复，CSS 约束不溢出 |
| 70 | 启动空状态标签页 | ✓ | v0.6.0：欢迎标签页（`isWelcome`），不在标签栏显示，真实标签页创建后自动隐藏 |
| 71 | 设置独立标签页 | ✓ | v0.6.0：软件设置从模态窗改为独立标签页（`type:'settings'`），与串口标签页并列切换 |
| 72 | `pageEl` 统一访问 | ✓ | v0.6.0：标签页元素通过 `pageEl(id)` → `getActivePage()` 路由，全局元素回退 `document.getElementById` |
| 73 | 弹窗 JS 化 | ✓ | v0.6.0：离线遮罩/通知弹窗/主题弹窗/串口详细设置弹窗均由 JS 动态构建，不再依赖静态 HTML |
| 74 | index.html 精简 | ✓ | v0.6.0：仅保留 4 个结构元素（菜单栏/标签栏/容器/状态栏），静态标签页 DOM 全部移除 |
| 75 | 客户端气泡靠左 | ✓ | v0.6.0：GUI/CLI/MCP 三色气泡从独立居中区域移入 daemon 状态行左侧 |
| 76 | 设置页侧边栏导航 | ✓ | v0.6.0：编码与显示/外观/存储与行为/语言四个独立面板，左侧导航切换 |
| 77 | 设置页气泡卡片 | ✓ | v0.6.0：编码格式/Hex显示设置/文本显示设置/整体外观/颜色主题/图标主题/存储与行为/界面语言共 8 个气泡 |
| 78 | 整体外观模式 | ✓ | v0.6.0：跟随系统/深色/浅色三选一，与主题选择分离 |
| 79 | 智能模式回退 | ✓ | v0.6.0：所选模式不在主题支持列表中时自动降级 |
| 80 | 创建示例文件夹 | ✓ | v0.6.0：主题/图标/语言气泡底部一键创建示例文件夹和文件 |
| 81 | 自定义下拉框裁剪修复 | ✓ | v0.6.0：Portal 到 body 避免 overflow:hidden 裁剪 |
| 82 | 设置页全 i18n | ✓ | v0.6.0：31 个新翻译键，9 语言全覆盖 |
| 83 | 布局约束文档 | ✓ | v0.6.0：LAYOUT-RULES.md 记录各区域宽度策略 |
| 84 | 版本号统一 | ✓ | v0.6.0：全部 4 个 exe + index.html 统一版本号 |
| 85 | 时间行字节数 | ✓ | v0.6.0：历史消息时间行显示帧字节数 |
| 86 | Hex 无分割显示 | ✓ | v0.6.0：菜单新增无分割选项，Hex 连续无分隔显示 |
| 87 | 设置页标题在左侧导航顶部 | ✓ | v0.6.0：完全左右分栏，标题移入 `.settings-nav` 顶部 |
| 88 | 标签栏水平滚动 | ✓ | v0.6.0：`.tabs-scroll` 鼠标滚轮，`+` 固定右侧 |
| 89 | 关于移入设置页 | ✓ | v0.6.0：关于从弹窗改为设置页独立面板 |
| 90 | 尾部追加默认无 | ✓ | v0.6.0：appendSuffix 默认值从 crlf 改为 '' |
| 91 | 内置图标文件化 | ✓ | v0.6.0：`config/icons/system/` 20 个 SVG + icons.json（0.6.1 增 menu，0.6.2 增 reset/send） |
| 92 | 图标主题文件夹化 | ✓ | v0.6.0：`themes/icons/<名>/icons.json` + 平铺 SVG |
| 93 | 版本号统一 | ✓ | v0.6.0：`version/version.go` 一处修改全部同步 |
| 94 | 打开文件夹按键 | ✓ | v0.6.0：创建示例后增加打开系统资源管理器按键 |
| 95 | 设置导航项间距 | ✓ | v0.6.0：`margin-bottom: 2px; border-radius: 6px` |
| 96 | 连接管理加固 | ✓ | v0.6.1：`_connecting` 互斥防止并发重连，Go 侧锁区合并 + 身份校验防旧连杀新连，daemon 同 PID 清理 |
| 97 | 数据高亮颜色自定义 | ✓ | v0.6.1：分组列表式设置（时间戳/文本/Hex/特殊），深浅色独立/统一，调色板集成，预览气泡 |
| 98 | 离线遮罩范围精确化 | ✓ | v0.6.1：遮罩仅覆盖 `#mainContent` 页面区域，不挡标签栏和状态栏，设置页激活时自动隐藏 |
| 99 | 断连标签页清理 | ✓ | v0.6.1：`handleDaemonOffline` 完整销毁会话标签 DOM，重连时重建 |
| 100 | 日语专业重译 | ✓ | v0.6.1：全文件专业软件用语，敬体一致，母语无感化 |
| 101 | 文言文重译 | ✓ | v0.6.1：清除日文汉字混淆，统一用字风格 |
| 102 | 外置语言恢复与补全 | ✓ | v0.6.1：恢复 pt.json/lzh.json，补全全部新增键 |
| 103 | 菜单栏软件设置项 i18n | ✓ | v0.6.1：`menu.app_settings` 全 9 语言 |
| 104 | 整体外观图标 | ✓ | v0.6.1：三个模式按键 SVG 图标 |
| 105 | 颜色主题卡片优化 | ✓ | v0.6.1：两行布局、气泡加大、等宽 |
| 106 | 关于内容详细化 | ✓ | v0.6.1：四段式专业描述，9 语言 |
| 107 | 状态栏统计条件显示 | ✓ | v0.6.1：无会话/设置页时隐藏 |
| 108 | 时间戳字节统计 | ✓ | v0.6.1：`~` 前缀 + 独立颜色 `--dc-stat` |
| 109 | 高亮颜色扩展：背景色+加粗 | ✓ | v0.6.2：所有项增加 bgDark/bgLight/bold，双层列标题 |
| 110 | 控制字符单节点+::before渲染 | ✓ | v0.6.2：真字符在 DOM（color:transparent），CSS ::before 显示标记 |
| 111 | 转义字节格式可选 | ✓ | v0.6.2：`/FF`、`\xFF`、`0xFF`、`<FF>`、`[FF]` 五种格式下拉 |
| 112 | 复制链路重写 | ✓ | v0.6.2：cloneContents + 显式 `\n` 块间 → textContent，标记不入剪贴板 |
| 113 | 可见回车→可见控制字符 | ✓ | v0.6.2：菜单栏和设置页重命名，含 CR/LF/Tab/空格全套标记 |
| 114 | 允许复制转义字节开关 | ✓ | v0.6.2：默认开启，关闭后 `/FF` 不入剪贴板 |
| 115 | Tab 键输入支持 | ✓ | v0.6.2：发送框拦截 Tab 键插入 `\t` |
| 116 | 环形缓冲区快照读取 | ✓ | v0.6.3：`Snapshot()` 替代 `DrainAll()`，客户端读历史不清空缓冲区，多客户端可独立读取 |
| 117 | 历史清空本地化 | ✓ | v0.6.3：GUI 清空历史仅为本地视图操作，不调 daemon IPC，不清环形缓冲区，不清磁盘文件 |
| 118 | 上滚召回历史 | ✓ | v0.6.3：清空后上滚从 daemon 环形缓冲区快照召回数据；缓存边界外从磁盘文件翻页 |
| 119 | 磁盘历史翻页 | ✓ | v0.6.3：`SearchHistory` 分页回溯 `.log` 文件，含去重、offset 追踪、互斥防连点 |

## 网络调试

新增网络调试模块，支持 TCP/UDP 通信，复用现有串口工具的交互模式。

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | TCP Client | ❌ | 连接远程 TCP 服务器，文本/Hex 收发，自动发送，统计 |
| 2 | TCP Server | ❌ | 监听端口，接受客户端连接，广播/单播发送 |
| 3 | UDP 收发 | ❌ | 绑定本地端口，向目标地址发送，接收回包 |
| 4 | 与串口统一操作体验 | ❌ | 复用多标签页、文本/Hex 切换、自动发送、历史记录等全部交互 |
| 5 | 网络进程管理 | ❌ | 守护进程统一管理网络连接进程，与串口进程并列 |

## CLI

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | 独立可执行文件 | ✓ | `serial-cli.exe` |
| 2 | 命令行模式 | ✓ | CallOnce 临时管道 |
| 3 | 交互模式 | ✓ | 3 管道持久连接，REPL，支持引号参数 |
| 4 | 34 条命令 | ✓ | 含 declare / probe / setmode / forward 等；`history-*` 命令族 7 条；help 按功能分组重排 |
| 5 | start 命令 | ✓ | 统一启动入口，已运行则返回"已在运行中" |
| 6 | check 命令 | ✓ | 三层检测（进程+管道+IPC），tasklist 失效时回退 IPC |
| 7 | send --hex | ✓ | 十六进制发送，数据走共享内存 |
| 8 | monitor | ✓ | 持久 DaemonClient，3 管道收事件 |
| 9 | probe | ✓ | 设备端口探测，支持 --config 指定规则文件 |
| 10 | setmode | ✓ | 切换进程模式，需 idle 状态 |
| 11 | declare | ✓ | 声明端口配置不打开串口，支持完整参数和转发模式 |

## MCP

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | 独立可执行文件 | ✓ | `serial-mcp.exe`，JSON-RPC 2.0 over stdio |
| 2 | 28 个工具 | ✓ | 覆盖全部串口功能，含 serial_declare / serial_probe_ports / serial_set_mode / serial_multistr_* |
| 3 | serial_start_daemon | ✓ | 统一启动入口，已运行返回 already_running |
| 4 | serial_list_ports / serial_refresh_ports | ✓ | 端口列表缓存读取与强制硬件重扫 |
| 5 | serial_create / serial_open | ✓ | 创建空闲或连接进程（去重），支持 mode/portB 参数；open 为 create+connect 快捷方式 |
| 6 | serial_connect / serial_disconnect | ✓ | 进程生命周期管理 |
| 7 | serial_switch | ✓ | 切换进程到其他端口，保留进程与历史 |
| 8 | serial_forward_create | ✓ | 创建双向端口转发进程 |
| 9 | serial_set_mode | ✓ | 切换进程模式 single ↔ forward，需 idle 状态 |
| 10 | serial_declare | ✓ | 声明端口配置不打开串口，完整参数支持 |
| 11 | serial_close | ✓ | 销毁进程（有连接先断开） |
| 12 | serial_send（共享内存路径） | ✓ | 写共享内存后 IPC 触发 |
| 13 | serial_sessions / serial_status | ✓ | 进程列表与守护进程状态查询 |
| 14 | serial_history / serial_stats | ✓ | 历史缓冲区与 I/O 统计读取 |
| 15 | serial_monitor | ✓ | 持久 DaemonClient 监听事件 |
| 16 | serial_port_watch | ✓ | 轮询端口变化 |
| 17 | serial_autosend_start/stop/status | ✓ | 自动发送管理 |
| 18 | serial_sendqueue | ✓ | 写入多条到发送队列 |
| 19 | serial_multistr_save/load/status | ✓ | 多字符串持久化与状态查询 |
| 20 | serial_probe_ports | ✓ | 设备端口探测，可选端口/波特率/规则过滤 |
| 21 | serial_shutdown | ✓ | 优雅关闭守护进程 |

## Web 客户端

守护进程内嵌 HTTP server + WebSocket，可通过浏览器访问，`--web` 参数控制开关。

| # | 需求 | 状态 | 说明 |
|---|------|------|------|
| 1 | `--web` 启动参数 | ❌ | 守护进程新增 `--web :8080` 参数，未指定时不启动 HTTP 服务 |
| 2 | REST API | ❌ | HTTP 端点映射全部 IPC 方法（`POST /api/process.create` 等） |
| 3 | WebSocket 事件推送 | ❌ | 单条 WebSocket 替代 3 管道：请求/响应/事件全走 ws |
| 4 | Web 前端 | ❌ | 纯 HTML/JS/CSS，Go embed 内嵌，与现有前端共用样式 |
| 5 | 局域网访问 | ❌ | 内网任意设备浏览器操作串口，手机/平板可用 |
| 6 | 安全控制 | ❌ | `--web 127.0.0.1:8080` 仅本机；`--web 0.0.0.0:8080` 局域网，可选 token 认证 |
| 7 | 功能对等 | ❌ | Web 端覆盖现有 GUI 全部功能（多标签页、文本/Hex、自动发送、转发模式、设置持久化等） |

## Python 版对照

Python 版与 Go 版各有优劣，以下为功能对比：

| 功能 | Python 版 | Go 版 (v0.6.4) |
|------|-----------|-----------------|
| 端口自动发现 | ✅ `HELP\r\n` 探针 | ✅ TOML 规则探测 |
| MCP 协议 | ❌ | ✅ 28 工具 |
| GUI 桌面应用 | ❌ | ✅ Wails v2 |
| 自动发送引擎 | ❌ | ✅ 单条/队列模式 |
| 统计系统 (I/O 吞吐/速率) | ❌ | ✅ stats-count + stats-rate |
| 串口切换 | ❌ | ✅ 全客户端支持 |
| 进程模式切换 | ❌ | ✅ idle 状态下 single ↔ forward |
| 端口转发 | ❌ | ✅ 双向透传+监控，1/2 方向标签 |
| 端口声明 | ❌ | ✅ 声明配置不打开端口 |
| 设备探测 | ❌ | ✅ TOML 规则 + Modbus CRC |
| 多语言 | ❌ | ✅ 9 种语言 |
| 滚动锁定 | ❌ | ✅ 锁定/自动滚动 |
| Modbus RTU | ✅ | ❌ 待实现 |
| MCU 协议感知 (`#` 过滤) | ✅ | ❌ 待实现 |

Go 版已实现设备端口探测（可配置 TOML 规则，含 Modbus CRC 检测），Modbus RTU 完整协议支持和 MCU 协议感知仍待实现。
